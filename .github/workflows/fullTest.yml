name: Full test prior to release

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        java: [ 17, 21, 25 ]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v5
        with:
          java-version: '${{ matrix.java }}'
          distribution: 'temurin'

      - name: Install sbt
        uses: sbt/setup-sbt@v1

      - name: Cache Coursier cache
        uses: coursier/cache-action@v6

      - name: Run tests with Scala 2.13,3.3
        run: sbt +test +doc

      - name: Run tests with latest Pekko 1.x and Scala 3.3
        run: sbt -Dpekko.build.version=latestV1 test

      - name: Run tests with next major Pekko 2.x and Scala 3.3
        run: sbt -Dpekko.build.version=latestV2 test

